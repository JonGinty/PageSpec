import {describe, expect, test} from '@jest/globals';
import { objectKeysToLowerCase } from '../core/utils';

describe("utils", () => {
    test("object keys to lower case", async () => {
        const foo = {
            TestKey: "testval",
            tesTkEy2: "testval2"
        }

        
    })
})